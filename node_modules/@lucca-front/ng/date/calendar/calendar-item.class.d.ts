import { ELuDateGranularity } from '@lucca-front/ng/core';
import { ICalendarItem } from './calendar-item.interface';
export declare abstract class ACalendarItem<D> implements ICalendarItem<D> {
    get id(): string;
    date: D;
    mods: string[];
    isDisabled: boolean;
    label: string;
    readonly granularity: ELuDateGranularity;
}
export declare class DayItem<D> extends ACalendarItem<D> implements ICalendarItem<D> {
    date: D;
    label: string;
    readonly granularity = ELuDateGranularity.day;
    constructor(date: D, label: string);
}
export declare class MonthItem<D> extends ACalendarItem<D> implements ICalendarItem<D> {
    date: D;
    label: string;
    readonly granularity = ELuDateGranularity.month;
    constructor(date: D, label: string);
}
export declare class YearItem<D> extends ACalendarItem<D> implements ICalendarItem<D> {
    date: D;
    label: string;
    readonly granularity = ELuDateGranularity.year;
    constructor(date: D, label: string);
}
export declare class DecadeItem<D> extends ACalendarItem<D> implements ICalendarItem<D> {
    date: D;
    label: string;
    readonly granularity = ELuDateGranularity.decade;
    constructor(date: D, label: string);
}
