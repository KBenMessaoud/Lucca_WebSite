import { ChangeDetectionStrategy } from '@angular/core';
import { ILuPopupConfig } from '@lucca-front/ng/popup';
export type LuModalMode = 'modal' | 'sidepanel';
export interface LuModalClasses {
    panel: string;
    panelContainer: string;
    panelInner: string;
    overlayPane?: string;
}
export declare const luModalClasses: Record<LuModalMode, LuModalClasses>;
type LuModalModeWithPosition = {
    mode?: 'modal';
    position: undefined | 'center';
} | {
    mode: 'sidepanel';
    position: 'left' | 'right';
};
export type LuModalConfig = ILuPopupConfig & LuModalModeWithPosition & {
    changeDetection: ChangeDetectionStrategy;
};
/**
 * @deprecated Use LuModalConfig instead.
 */
export type ILuModalConfig = LuModalConfig;
export {};
