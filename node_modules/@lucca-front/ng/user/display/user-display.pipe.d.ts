import { PipeTransform } from '@angular/core';
import { ILuUser } from '../user.model';
import { LuDisplayFormat } from './display-format.model';
import * as i0 from "@angular/core";
/**
 * Displays a user name according to specified format. Supported formats: f for first name,
 * F for first initial, l for last name, L for last initial.
 */
export declare function luUserDisplay(user: ILuUser, format?: LuDisplayFormat): string;
/**
 * Displays a user name according to specified format. Supported formats: f for first name,
 * F for first initial, l for last name, L for last initial.
 */
export declare class LuUserDisplayPipe implements PipeTransform {
    private readonly defaultFormat;
    transform(user: ILuUser, format?: LuDisplayFormat): string;
    static ɵfac: i0.ɵɵFactoryDeclaration<LuUserDisplayPipe, never>;
    static ɵpipe: i0.ɵɵPipeDeclaration<LuUserDisplayPipe, "luUserDisplay", true>;
}
