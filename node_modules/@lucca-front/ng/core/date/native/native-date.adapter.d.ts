import { ALuDateAdapter } from '../date-adapter.class';
import { ILuDateAdapter } from '../date-adapter.interface';
import { ELuDateGranularity } from '../date-granularity.enum';
import { ILuNativeDateAdapterOptions } from './native-date.option';
import * as i0 from "@angular/core";
export declare class LuNativeDateAdapter extends ALuDateAdapter<Date> implements ILuDateAdapter<Date> {
    private _locale;
    private _options;
    private _regex;
    private _order;
    constructor(_locale: string, _options: ILuNativeDateAdapterOptions);
    private initOrder;
    private extract;
    isParsable(text: string, granularity?: ELuDateGranularity): boolean;
    parse(text: string, granularity?: ELuDateGranularity): Date;
    format(d: Date, format: string): string;
    forge(year: number, month: number, date: number): Date;
    forgeToday(): Date;
    forgeInvalid(): Date;
    isValid(d: Date): boolean;
    clone(d: Date): Date;
    getYear(d: Date): number;
    getMonth(d: Date): number;
    getDate(d: Date): number;
    getDay(d: Date): number;
    add(d: Date, count: number, granularity: ELuDateGranularity): Date;
    static ɵfac: i0.ɵɵFactoryDeclaration<LuNativeDateAdapter, [null, { optional: true; }]>;
    static ɵprov: i0.ɵɵInjectableDeclaration<LuNativeDateAdapter>;
}
